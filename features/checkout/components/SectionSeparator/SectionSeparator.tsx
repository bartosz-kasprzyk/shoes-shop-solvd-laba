import { Box } from '@mui/material';

export default function SectionSeparator() {
  return (
    <Box
      sx={{
        width: '100%',
        height: '1px',
        bgcolor: '#EAECF0',
        my: 6,
      }}
    />
  );
}
